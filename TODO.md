# TODO

ListUp 작업 목록. **이 파일이 유일한 기준**이며, 작업할 때마다 갱신한다.

## 진행 규칙

- 상태: `[ ]` 대기 / `[~]` 진행 중 / `[x]` 완료
- 완료한 항목은 지우지 말고 맨 아래 **완료** 절로 옮긴다 (무엇을 왜 했는지가 남아야 한다).
- 새 항목은 **왜 필요한지 한 줄**을 같이 적는다. 이유 없는 항목은 나중에 판단이 안 된다.
- 우선순위는 `P0`(운영 전 필수) → `P3`(여유될 때) 순.
- 코드 위치를 아는 항목은 `파일:줄` 을 적어 둔다.

## 현재 작업 환경

PC 실행이 가능하다. 코드를 만지면 테스트·타입 검사를 바로 돌린다.

- 이 PC는 Node 24 인데 better-sqlite3 11.x 는 Node 24 용 프리빌드 바이너리가 없고,
  Windows SDK 가 없어 소스 빌드도 안 된다. 12.x 로 올려 해결. → `server/package.json`
- 마지막으로 검증된 지점: 개선 작업 커밋 (2026-08-27).
  서버 테스트 106개 통과, 서버·앱 타입 검사 통과, 실제 서버 기동 후 API 왕복·413·ETag/304·
  경로 충돌 409·정적 서빙·보안 헤더·닷파일 차단·설정값 검증·백업 명령까지 직접 확인.

---

## P0 — 운영 전에 반드시

- [ ] **참조되지 않는 blob 정리(GC)**
  제안에 담기 전에 버려진 업로드가 디스크에 영원히 남는다. 지금은 지우는 경로가 아예 없다.
  `snapshot_entries` 와 `proposal_changes` 어느 쪽에서도 참조하지 않는 `blobs` 를 찾아
  일정 시간(예: 24시간) 지난 것만 삭제. → `server/src/lib/storage.ts` 의 `remove()` 는 이미 있음.
  `repo_blobs` 에 업로드 시각이 남으므로 이를 기준으로 삼으면 된다.
- [ ] **비밀번호를 바꿔도 기존 토큰이 계속 유효함**
  토큰이 무상태(HMAC)라 유출된 토큰을 비밀번호 변경으로 끊을 수 없다.
  `users` 에 `token_epoch` 를 두고 토큰 페이로드에 실어 비교하는 방식이 가장 가볍다.
  → `server/src/routes/auth.ts` (`/auth/password`), `server/src/lib/auth.ts`
- [ ] **로그인 요청 제한(rate limit) 없음**
  비밀번호 대입 공격을 막을 장치가 전혀 없다. IP + 이메일 단위로 실패 횟수를 세고 지연/차단.
  선행 작업이던 `trustProxy` 는 들어갔으므로 터널 뒤에서도 실제 IP 를 쓸 수 있다.
- [ ] **운영 배포 문서 마무리**
  환경변수 표·백업 명령은 README 에 넣었다. 남은 것: 재시작·업데이트 절차, 데이터 디렉터리
  보관 위치, 장애 시 확인 순서를 한 곳에 정리.

## P1 — 다음에 할 것

- [ ] **알림(웹/네이티브)이 `window.alert` 와 `Alert.alert`**
  업로드 실패 같은 안내가 모달로 흐름을 끊는다. 화면 안 토스트로 바꾼다.
- [ ] **파일 미리보기** — 지금은 무조건 내려받아야 내용을 볼 수 있다.
  이미지·평문 정도는 앱 안에서 바로 보여 준다. 서버의 `?inline=1` 은 이미 준비되어 있다.
- [ ] **딥링크로 초대 참여** — `scheme: "listup"` 과 `join?code=` 처리는 되어 있으나
  실제 링크(`listup://join?code=...`, 웹 URL)를 만들어 공유하는 경로가 없다.
  초대 화면의 "코드 복사"를 "링크 복사"와 함께 제공.
- [ ] **CI (`.github/workflows` 없음)** — 최소한 `npm test` + `npm run typecheck`.
  테스트가 106개까지 늘었으니 이제 자동으로 돌릴 값어치가 있다.

## P2 — 사용성

- [ ] **폴더 통째로 내려받기(zip)** — 파일이 많으면 하나씩 받는 게 현실적이지 않다.
- [ ] **제안 목록 페이지네이션** — 현재 전체를 한 번에 불러온다.
  (변경 이력은 커서 방식으로 바꿨으니 같은 방식을 쓰면 된다.)
- [ ] **용어·문구 통일** — "변경 제안하기/새 제안/변경 제안 만들기/제안 올리기", "병합/반영",
  가입 화면 제목("회원가입" vs "계정 만들기"), 만료일 표기 형식이 화면마다 다르다.
- [ ] **`docs/API.md` 와 실제 라우트 재대조** — 이번 변경(커서·용량 한도·ETag·400·`repo_blobs`)은
  반영했지만, 전체를 라우트별로 다시 훑은 적은 없다.
- [ ] **앱 접근성 마무리** — 목록 행에는 역할·라벨을 넣었지만 스크린리더로 실제 확인은 못 했다.
  라이트 테마 `accent`(#2f6df6) 는 `accentSoft`/`bg` 위에서 3.95/4.22 로 아직 AA 미달
  (활성 탭·필터 칩 글자). 다크는 통과.

## P3 — 나중에

- [ ] 제안이 올라오면 알림(푸시)
- [ ] 스토리지 백엔드 S3 호환 어댑터 (`BlobStore` 인터페이스만 교체하면 되도록 설계해 둠)
- [ ] 이메일 인증 / 비밀번호 재설정
- [ ] 앱 단위 테스트 (지금은 서버 테스트만 있음)
- [ ] 저장소 검색 / 파일 검색
- [ ] 제안에 이미 담긴 파일을 다시 담을 때 교체할지 묻기
- [ ] **마이그레이션이 `foreign_keys=ON` 트랜잭션 안에서 실행** — 테이블 재작성이 필요한 변경을
  못 한다. 마이그레이션 동안만 FK 를 끄고 끝에 `PRAGMA foreign_key_check`. `server/src/db/index.ts`
  (데이터 변환용 `run(db)` 훅은 이번에 들어갔다.)
- [ ] **다운로드 Range/Accept-Ranges 없음** — 미리보기(스트리밍)를 만들 때 같이.
- [ ] **LICENSE 파일·`license` 필드 없음** — 공개 저장소인데 라이선스가 없다. 어떤 라이선스로 할지
  결정 필요(소유자 판단).
- [ ] **웹 번들 1.7MB** — 아이콘 글리프맵 전 종류가 들어간다. 압축은 터널(Cloudflare)이 해 준다.
- [ ] **라우트 보일러플레이트** — 권한 확인·파싱이 라우트마다 반복된다. 스키마 검증 도입은 취향 문제.
- [ ] **제안 목록·상세의 `conflicts` 설명** — 이제 "제안 뒤 바뀐 파일" 말고 "파일/폴더 이름 충돌"도
  섞여 들어가는데, 앱 배너 문구는 앞의 경우만 설명한다.

---

## 검증 대기

### 최신 검증 — 개선 작업 (2026-08-27)

- [x] `npm test` — 서버 테스트 106개 통과 (66 → 106)
- [x] `npm run typecheck` — 서버 + 앱 통과
- [x] 실제 서버 기동 후 직접 확인: 가입 → 저장소 생성 → 업로드 → 다운로드,
      한도 초과 업로드 413(아무것도 커밋 안 됨), ETag + `If-None-Match` 304,
      비밀번호 오류 400, 파일/폴더 이름 충돌 409, 정적 서빙 + 보안 헤더,
      `/.env` 404, SPA 라우트 200, 잘못된 `LISTUP_PORT` 로 기동 실패,
      모르는 `LISTUP_*` 경고, `npm run backup`
- [ ] `npm run build:web --workspace @listup/app` — 이번 변경 뒤로는 안 돌려 봤다
- [ ] 브라우저 흐름 재확인 — 인증 가드·보호 라우트·서버 주소 화면이 새로 들어갔다

### 클린 클론 검증 — `83b861f` 기준 통과

푸시된 브랜치를 새로 clone 해서 처음부터 돌린 결과. 남이 받아서 실행해도 되는 상태다.

- [x] `npm install` — 약 38초, 실패 없음
- [x] `npm test` — 서버 테스트 66개 통과
- [x] `npm run typecheck` — 서버 + 앱 통과
- [x] `npm run build:web --workspace @listup/app` — 웹 번들 생성 성공
- [x] `npm run server` 기동 후 API 왕복 확인
- [x] 브라우저 흐름 (가입 → 저장소 생성 → 업로드 → 초대 → 참여 → 제안 → 병합) — `78645f4` 시점

### 실기기 — Android 외부망 (2026-08-27)

폰을 다른 네트워크(LTE)에 두고 확인했다. 공개 경로 두 개를 열었다:
API·웹은 Cloudflare 빠른 터널, Metro 는 Expo 터널(`npm run app -- --tunnel`, `@expo/ngrok`).

- [x] Expo Go 로 앱이 뜨고 동작함 — **처음으로 네이티브에서 실행해 봤다**
- [x] Android Chrome 에서 웹앱 접속
- [x] **서버 주소 화면** — 터널 모드에서는 접속 주소(`…exp.direct`)로 API 를 유추할 수 없으므로
      앱에서 직접 지정해야 하는데, 이 경로가 실제로 동작했다. 이 화면을 만든 이유가 이것이다.
- [x] 공개 주소로 API 왕복 (가입 → 저장소 → 한글 파일 업로드 → 되읽기 → 초대 발급)

- [ ] `expo-document-picker` 로 고른 파일이 네이티브에서 업로드되는지 — **개별 확인 안 됨**
- [ ] `File.downloadFileAsync` + 공유 시트로 저장이 되는지 — **개별 확인 안 됨**
- [ ] 다크 모드 표시 — **개별 확인 안 됨**
- [ ] 같은 공유기(LAN)에서 서버 주소 자동 감지가 동작하는지 — 외부망으로 확인해서 이 경로는 미검증

> 위 세 항목은 "전체적으로 잘 된다"는 확인만 받았고 하나씩 짚어보지는 않았다.
> 파일 선택·저장(`app/src/lib/files.ts`)은 웹과 네이티브 구현이 갈리는 지점이라
> 따로 확인하는 편이 안전하다.

### 로컬에서 받아 돌리는 법

```bash
git clone -b claude/file-sharing-collab-platform-tvo6dg https://github.com/yh-yeop/ListUp.git
cd ListUp && npm install
npm run server     # 터미널 1 — http://localhost:4000
npm run app        # 터미널 2 — 브라우저는 w, 모바일은 QR
```

Node 20.11 이상이 필요하다. `better-sqlite3` 는 네이티브 모듈이지만 12.x 부터는
Node 20~26 프리빌드 바이너리를 제공하므로 보통은 그냥 설치된다 (11.x 시절에는 Node 24 에서
빌드 도구가 없으면 설치가 깨졌다 — 그래서 12.x 로 올렸다). 프리빌드를 못 받는 특수한
환경에서만 빌드 도구(Windows SDK 등)가 필요하다.

---

## 완료

- [x] 서버 기반 (SQLite 스키마·마이그레이션, 콘텐츠 주소 blob 저장, scrypt + HMAC 토큰) — `2a5d386`
- [x] 저장소 / 파일 / 스냅샷 API — `2a5d386`
- [x] 초대 코드 API (발급·미리보기·참여·회수, 만료·횟수 제한) — `2a5d386`
- [x] 변경 제안 API (생성·댓글·충돌 감지·병합·닫기) — `2a5d386`
- [x] 서버 테스트 66개 — `2a5d386`
- [x] Expo 앱 화면 일습 (로그인·저장소·파일·초대·제안·멤버·이력) — `78645f4`
- [x] 문서 (README, `docs/ARCHITECTURE.md`, `docs/API.md`) — `78645f4`
- [x] better-sqlite3 `^11.5.0` → `^12.2.0` — Node 24 용 프리빌드가 없어 `npm install` 이
  깨지던 문제. 서버 테스트 66개 통과로 호환 확인. — 2026-08-19
- [x] 웹 정적 빌드를 서버가 같은 오리진으로 서빙 — `@fastify/static` + SPA fallback,
  `LISTUP_WEB_DIR` 로 조정. `EXPO_PUBLIC_LISTUP_API_URL=/` 빌드와 조합하면 프로세스 하나로
  웹+API 가 나가서 배포가 단순해진다. — 2026-08-19
- [x] 첫 공개 배포 (Cloudflare 빠른 터널) — 공개 URL 에서 가입·로그인·저장소 생성·업로드·
  되읽기·초대 발급 왕복과 웹 로그인 흐름까지 확인. `deploy-tunnel.ps1` 추가. — 2026-08-19

- [x] **코드 리뷰 기반 개선 묶음** — 2026-08-27. 8개 관점 리뷰 → 회의적 검증 → 수정 → 재리뷰.
  테스트 66 → 106개.

  *서버*
  - 업로드 한도 초과 파일이 잘린 채 커밋되던 P0 버그 (multipart 가 에러 없이 자르고 `truncated`
    만 세우는 것을 검사) → 413, 아무것도 커밋 안 됨
  - 동시 업로드 lost update — 트랜잭션 안에서 head 재조회
  - `repo_blobs` 도입 — 해시만 알면 남의 저장소 파일을 제안으로 읽을 수 있던 문제 차단
  - 저장 용량 한도 (저장소 총량 / 사용자별 하루 스테이징), 병합 시 파일 수 한도 재검사
  - 경로 기준 `/raw` 에 ETag + `no-cache`, `snapshot` 지정 시에만 immutable, HEAD 는 스트림 안 엶
  - 파일/폴더 이름 충돌 검사(`findPathConflict`)를 업로드·이동·제안 생성·병합에 적용
  - `normalizePath` NFC 정규화 + 세그먼트 trim + format 문자 거부, 기존 데이터 마이그레이션(v3)
  - scrypt 비동기화, 없는 계정도 더미 해시 검증(응답 시간 노출 차단),
    비밀번호 오류 401 → 400 (앱이 로그아웃하던 문제)
  - 추방·강등된 사람의 초대 무효화 + 자동 회수
  - blob 쓰기 `flush`, 크기 불일치 시 교체, 기동 시 임시 파일 정리
  - 설정값 검증(잘못되면 기동 실패), 모르는 `LISTUP_*` 경고, `trustProxy`, 로그 레벨,
    초대 코드 로그 마스킹, 종료 타임아웃, 500 응답에 `requestId`
  - CORS 목록 trim + credentials 제거, 정적 서빙 닷파일 차단·캐시 헤더·보안 헤더
  - `npm run backup` (WAL 안전 백업), `tsx` 를 dependencies 로, FK 자식 인덱스,
    이력 커서를 `(created_at, id)` 복합으로
  - `deploy-tunnel.ps1` 헬스체크 대기 + 잘못 빌드된 번들 자동 재빌드

  *앱*
  - 토큰 복원 중 401 로 로그아웃되던 P0 버그, 오프라인이어도 세션 유지, 인증 가드(보호 라우트)
  - 서버 주소 변경 화면 (연결 확인 후 저장, 주소가 바뀌면 세션 삭제 — 토큰이 다른 호스트로
    가지 않게), 릴리스 빌드에서 서버를 바꿀 수 있게 됨
  - 저장소 이름·설명 수정, 소유권 넘기기, 파일·폴더 이름 변경, 과거 시점 보기, 이력 더 보기
  - 권한에 맞는 버튼 노출(제안 닫기·초대 회수), 병합된 제안은 제안 당시 원본을 내려받게
  - 업로드 사전 검사(서버 한도 조회), 덮어쓰기 확인, 실패 원인 표시
  - 제안 작성 중 이탈 경고, 탐색 중인 폴더에 담기, 중복 파일 key 충돌
  - 대비 AA 충족(textMuted/textFaint/warning), 입력 포커스 표시, 접근성 역할·라벨,
    요청 타임아웃 30초, 죽은 코드 제거

- [x] **외부망 Android 실기기 확인** — 2026-08-27. Cloudflare 터널(API·웹) + Expo 터널(Metro)로
  다른 네트워크의 폰에서 접속. 네이티브 앱을 처음으로 실행해 봤고, 터널 모드에서 필수인
  "서버 주소" 화면이 실제로 제 몫을 했다. `npm run app` 이 LAN IP 를 직접 고르고
  (`scripts/start-app.mjs`) `-- --tunnel` 을 넘길 수 있게 정비했다.

### 고친 버그 (기록)

- 초대 코드가 소진된 뒤 **이미 참여한 사람이 다시 열면 거부**되던 문제.
  사용 가능 여부를 멤버 확인보다 먼저 보고 있었다. 순서를 뒤집어 해결. — `2a5d386`
- 가로 스크롤 탭(`RepoNav`)이 세로로 늘어나 **아래 버튼 클릭을 가로막던** 레이아웃 문제. — `78645f4`
- Git Bash 에서 `EXPO_PUBLIC_LISTUP_API_URL=/` 로 웹을 빌드하면 MSYS 경로 변환이 `/` 를
  `C:/Program Files/Git` 로 바꿔 번들에 박히던 문제. PowerShell 에서 빌드하도록 README 에
  명시해 회피. Metro 캐시는 환경변수 변경을 못 알아채므로 `--clear` 가 필요했다. — 2026-08-19
