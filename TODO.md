# TODO

ListUp 작업 목록. **이 파일이 유일한 기준**이며, 작업할 때마다 갱신한다.

## 진행 규칙

- 상태: `[ ]` 대기 / `[~]` 진행 중 / `[x]` 완료
- 완료한 항목은 지우지 말고 맨 아래 **완료** 절로 옮긴다 (무엇을 왜 했는지가 남아야 한다).
- 새 항목은 **왜 필요한지 한 줄**을 같이 적는다. 이유 없는 항목은 나중에 판단이 안 된다.
- 우선순위는 `P0`(운영 전 필수) → `P3`(여유될 때) 순.
- 코드 위치를 아는 항목은 `파일:줄` 을 적어 둔다.

## 현재 작업 환경

PC 실행이 가능하다. 코드를 만지면 테스트·타입 검사를 바로 돌린다.

- 이 PC는 Node 24 인데 better-sqlite3 11.x 는 Node 24 용 프리빌드 바이너리가 없고,
  Windows SDK 가 없어 소스 빌드도 안 된다. 12.x 로 올려 해결. → `server/package.json`
- 마지막으로 검증된 지점: **v1.0.0** (2026-08-27).
  서버 테스트 118개 통과, 서버·앱 타입 검사 통과, 클린 클론 설치·테스트 통과,
  실서버에서 P0 3종(로그인 제한·토큰 세대·blob GC)과 전체 협업 흐름 직접 확인.
- 그 뒤 **서버 목록** (2026-09-14, 미커밋 시점 기준): 서버 테스트 118개·타입 검사 통과,
  웹 빌드를 Edge 로 서버 두 개에 걸어 35개 확인 3회 연속 통과. 네이티브(Expo Go)는 아직.
- **v1.2.0 준비** (2026-09-14): 서버 테스트 150개, 타입 검사, 웹 흐름 테스트 147개(`npm run test:e2e`)
  통과. 네이티브 전용 경로(조각 읽기·폴더 선택·다운로드 작업·secure-store)는 아래 *검증 대기 — v1.2.0 폰*.
- **지금 (2026-09-16):** 최신 릴리즈 **v1.2.1** (안드로이드 내려받기를 저장 폴더로). 서버 데이터는 2026-09-14 에
  비우고 새로 시작 — 음악 서버·`music@listup.local` 은 없다. 테스트 서버는 **https://listup.tailffc023.ts.net**
  (Tailscale Funnel, `npm run serve -- --tunnel`), 테스트 계정·저장소·초대가 하나씩 있다. 폰에서 v1.2 체크리스트
  (*검증 대기 — v1.2.0 폰*) 진행 중: 초대 링크로 가입·참여, 조각 업로드는 확인됨.
- **v1.3.0 (2026-09-17):** PC 앱(클라이언트 + 이 PC 에서 서버 열기), 내 폴더와 비교, 터널 뒤 로그인 제한 수정.
  릴리즈 노트 `docs/releases/v1.3.0.md`. 사용자가 PC 앱을 설치해 서버 켜기까지 써 보고 "잘 된다"고 확인(2026-09-16).
- **PC 앱 (2026-09-16):** 설치형 클라이언트 + **이 PC 에서 서버 열기**를 한 앱으로(아래 ④). 서버 테스트 157개,
  타입 검사(서버·앱·desktop), 웹 흐름 147개, PC 앱 흐름 21개(`npm run test:desktop`)를 개발 묶음과 **설치본
  (win-unpacked)** 양쪽에서 통과. 설치 파일 `release/listup-1.2.1-windows-setup.exe`(115MB)는 만들었지만
  **실제로 설치해 보지는 않았다** → *검증 대기 — PC 앱*.
- **다음 후보:** ① 폰 체크리스트 마저(v1.2 + 내 폴더와 비교) ② PC 앱 검증 대기 항목.
  보안은 2026-09-17 에 논의하고 지금 수준으로 두기로 했다(나중 후보: "Tailscale 안에서만" 공개 방식, 2단계 인증,
  종단간 암호화는 비용이 큼).
  (터널 뒤 로그인 제한 문제는 2026-09-16 고침, 완료 절.)

---

## P0 — 운영 전에 반드시

**1.0 기준으로 비어 있다.** 새로 생기면 여기에 적는다.

## P1 — 다음에 할 것

- [ ] **알림(웹/네이티브)이 `window.alert` 와 `Alert.alert`**
  업로드 실패 같은 안내가 모달로 흐름을 끊는다. 화면 안 토스트로 바꾼다.
- [ ] **폴더 올리기 중단 버튼** — 진행률은 보이지만 도중에 멈출 수단이 없다. 세션은 GC 가 치운다.

### 설치형 클라이언트 — 서버와 무관하게 늘 켜지는 클라

**실행 계획: [docs/PLAN-CLIENT.md](docs/PLAN-CLIENT.md)** (2026-09-14)

서버 목록을 만들었지만 그 목록을 여는 화면이 아직 서버에 묶여 있다. 웹 클라이언트는 서버가
서빙하므로 서버가 꺼지면 메뉴도 없고, 서버 목록은 브라우저 저장소에 주소(origin)별로 들어가
빠른 터널 주소가 바뀌면 빈 목록으로 시작한다. 폰의 Expo Go 는 PC 의 Metro 가 켜져 있어야 뜬다.
마인크래프트처럼 **클라이언트는 설치해 두고, 서버 목록은 클라이언트가 들고, 들어갈 때만 서버에
붙는** 구조가 필요하다.

**결정 (2026-09-14):** 폰은 **이 PC 에서 직접 빌드한 APK** (Expo 계정 안 씀, JDK 17·Android SDK
설치), PC 는 **설치형 앱** (웹 클라이언트는 https 라 LAN 의 http 서버에 못 붙어서 뺌).

- [x] **① 클라이언트 모드** — 2026-09-14. 설치형에는 "앱을 준 서버"가 없다. `IS_CLIENT_BUILD`
      (릴리스 네이티브 또는 `EXPO_PUBLIC_LISTUP_CLIENT=1` 웹 빌드)면 기본 서버를 두지 않고, 로그인돼
      있지 않으면 늘 서버 목록(로그아웃·세션 만료·지금 서버 삭제 뒤에도). 클라이언트 모드 웹을
      ListUp 과 무관한 정적 서버에 올려 21개 확인 3회, 서버 모드 회귀 35개 2회 통과.
- [x] **② 버전 호환 확인** — 2026-09-14. 따로 설치한 클라이언트는 다른 버전 서버에 붙는다.
      `/api/health` 에 `apiVersion`(`shared` 의 `API_VERSION` = 1)·`version` 을 더했고(서버 테스트
      +1, 119개), 다르면 서버 추가·전환을 막고 이유를 보여준다. 필드가 없는 1.0.0 서버는 1 로 본다.
      목록을 열면 서버마다 핑을 보내 상태 배지를 단다. 버전이 다른 가짜 서버로 11개 확인.
- [~] **③ 안드로이드 APK** — 2026-09-14 **빌드 성공, 실기기 설치는 아직.** `npm run build:android` →
      `release/listup-<버전>-android.apk` (arm64, 40.8MB, 첫 빌드 17분). 릴리스 키 서명·
      `usesCleartextTraffic`·targetSdk 36 을 APK 에서 확인. `android/` 는 커밋 안 함(매번 prebuild).
      **서명 키 `~/.listup/listup-release.keystore` + `android-signing.json`(비밀번호 평문)은 저장소
      밖에 있고, 잃으면 같은 앱으로 업데이트 설치가 안 된다 → 백업 필요.**
      **실기기 (2026-09-14):** 폰에 설치해 앱이 켜지고, 음악 서버를 추가해 들어가는 것까지 확인.
      남은 것: 공유기 주소(http)와 터널(https)을 각각 확인, 파일 선택·저장, 새 APK 로 업데이트 설치.
- [~] **로그인 정보 저장 (클라이언트의 비밀번호 관리자)** — 2026-09-14. 서버마다 이메일·비밀번호를
      기기에 저장하고, 토큰이 만료·폐기되면 저장된 계정으로 자동 다시 로그인한다.
      → `app/src/lib/credentials.ts`
      - **OS 가 암호화하는 곳에만 둔다.** 네이티브는 `expo-secure-store`(안드로이드 키 저장소), PC 앱은
        창의 `window.listupDesktop.credentials`(④에서 Electron safeStorage 로 제공). **서버가 주는 웹에는
        저장하지 않는다** — 브라우저 저장소가 평문이라, 브라우저 비밀번호 관리자에 맡긴다.
      - **직접 로그아웃한 서버는 자동 로그인하지 않는다**(`ServerEntry.signedOut`). 채워 둔 채 한 번 누르게 —
        같은 서버에 다른 계정으로 들어갈 길을 남긴다.
      - **자동 로그인이 비밀번호 오류(401)로 실패하면 저장된 비밀번호를 지우고 이메일만 남긴다.** 앱을 열
        때마다 틀린 비밀번호로 시도하면 서버의 로그인 실패 제한에 걸려 본인이 막히기 때문. 429·오프라인은 그대로.
      - **서버 주소를 고치면 비밀번호를 지운다.** 잘못 넣은 다른 서버로 비밀번호가 자동 전송되지 않게.
      - 앱에서 비밀번호를 바꾸면 저장값 갱신, 서버를 지우면 함께 삭제, 설정에서 따로 지우기.
      - 확인: 클라이언트 모드 웹에 가짜 PC 저장소를 넣어 28개. **폰(`expo-secure-store`)은 새 APK 로 확인 필요.**
- [~] **④ PC 앱 — 클라이언트 + 이 PC 에서 서버 열기** — 2026-09-16. **실제 설치 확인 전.** `desktop/` (Electron 44).
      서버를 여는 사람도 Node 설치·`npm install`·터미널 없이 — 서버 목록 맨 위 "이 PC 에서 서버 열기"로 켜고, 관리
      화면에서 공개 방식(Tailscale Funnel / 빠른 터널)·재설정 코드·자동 켜기·데이터 폴더·백업·로그. 두 앱으로
      나누지 않고 합쳤다(설치 한 번, "이 PC" 가 목록에 바로 이어짐). 구조는 `docs/ARCHITECTURE.md` *PC 앱*.
      - **먼저 시험한 것:** `app://listup`(standard, secure:false) 에서 localhost·공유기 IP(http)·Funnel(https)
        요청에 인증 헤더를 붙여도 200, localStorage 는 앱을 다시 켜도 남음. better-sqlite3 12 는 Electron 44 용
        프리빌드가 없어(ABI 149) **13 으로 올렸다** — N-API 라 한 바이너리가 Node·Electron 공용, 설치 스크립트도 없음.
      - 서버는 utilityProcess 에서 `startServer`(index.ts 에서 뺐다). 창을 닫아도 서버가 돌면 트레이에 남는다.
      - 초대 링크: "이 PC"(localhost)로 들어가 있으면 공개 주소, 없으면 공유기 안 주소로 만든다.
      - 설치본에서 걸린 것: electron-builder 가 extraResources 의 node_modules 를 걸러 서버가 뜨자마자 죽었다 →
        afterPack 에서 넣는다. 스테이징을 저장소 안에 두면 루트 워크스페이스를 알아보므로 임시 폴더에.
      - `npm run desktop`(개발), `npm run build:desktop`(설치 파일), `npm run test:desktop`(21개, `--app <exe>` 로 설치본).
        `release:publish` 가 설치 파일도 올린다(Windows 에서만). CI 는 Electron 실행 파일을 받지 않는다.
      - 하지 않은 것: 코드 서명(SmartScreen 경고), 자동 업데이트(릴리즈 페이지로), 이름 있는 Cloudflare 터널(명령줄
        서버에서), macOS·Linux 빌드.
- [x] **앱 아이콘** — 2026-09-14. 로그인 화면 로고(파란 둥근 사각형 + 흰 folder-open)와 같은 모양.
      `python scripts/make-icons.py` 가 앱이 쓰는 Ionicons 폰트에서 그려 `app/assets/` 에 icon(1024, 불투명)·
      adaptive-icon(투명, 안전 원 안에 — 원·둥근 사각형 런처에서도 안 잘림, 안드로이드 13 테마 아이콘 겸용)·
      favicon(웹 탭) 을 만든다. APK 에 적응형·단색 아이콘, 웹 빌드에 favicon.ico 가 들어간 것을 확인.
      **올라가 있는 v1.1.0 APK 에는 아이콘이 없다** — 다음 릴리즈부터.
- [~] **⑤ 배포** — 2026-09-14. **APK 는 GitHub 릴리즈에 올린다.** `npm run release:publish` 가 확인(깨끗한
      작업 트리·푸시됨·태그 없음·릴리즈 노트 `docs/releases/v<버전>.md`·버전 일치) → 서버 묶음·APK 빌드 →
      태그·릴리즈를 한 번에. 버전은 `npm run version:set -- <버전>` 으로 네 package.json·lock·app.json·
      `versionCode`(major*10000+minor*100+patch, 올라가야 안드로이드가 업데이트를 받는다)를 맞춘다.
      README 에 APK 설치법. PC 설치 파일은 ④ 뒤.
- [x] **앱 업데이트 알림** — 2026-09-14. 설치형 클라이언트가 앱을 열 때 GitHub `releases/latest` 와 자기 버전을
      견줘 새 버전이면 **버전마다 한 번** 제안하고, 설정에서는 계속 "새 버전 받기". **앱이 서버보다 오래돼
      들어갈 수 없으면(API 버전) 릴리즈 페이지를 바로 연다.** 로그인 없는 GitHub API 는 IP 당 시간 60번이라
      12시간에 한 번만 묻는다. 서버가 주는 웹은 묻지 않는다. 저장소는 `app.json` 의 `extra.updateRepo`
      (포크는 여기를 바꾼다). → `app/src/lib/updates.ts`. GitHub API 를 가짜로 가로채 12개 확인.

### 서버별 계정에서 따라오는 것

- [ ] **서버가 이름을 알려주기** (서버 목록 계획의 선택 단계 5, 하지 않았다) — 목록에서 이름을
  안 붙인 서버는 주소로 보인다. `/api/health`(`server/src/app.ts`)에 `serverName` 을 더하고
  `LISTUP_SERVER_NAME` 으로 주인이 정하게 하면, 추가 폼이 그 이름을 기본값으로 제안할 수 있다.
  지금은 사용자가 이름을 직접 붙인다.

- [x] **초대 링크 하나로 가입·참여**, **비밀번호 재설정 코드** — 2026-09-14, 아래 완료 절 *파일 공유 다듬기*.
  재설정 코드는 저장소 소유자가 아니라 **서버를 돌리는 사람이** 발급하는 것으로 바꿨다(소유자에게 남의
  계정을 풀 권한을 주면 안 된다 — 그 사람은 다른 저장소에도 속해 있다).

### 고정 주소 — 계속 쓰려면 (배포 대비)

지금은 켤 때마다 주소가 바뀐다(`*.trycloudflare.com`). 폰과 다른 PC 에 매번 다시 입력해야
하므로 "가끔 써보는 것"에서 "계속 쓰는 것"으로 넘어가는 길목을 막고 있다.

Cloudflare 가 로그에 직접 적어 두었듯, 빠른 터널은 **계정 없이 쓰는 시험용**이고
가동 보장이 없다. 계속 쓸 사람은 자기 계정으로 옮겨야 한다.

- [x] **① 안내** — README 5·6번. 도메인 유무로 갈라지는 두 경로를 적었다.
- [x] **② 적용 기능** — `scripts/tunnel.mjs` 가 준비 상태를 판별하고(quick / named /
      tailscale), `scripts/serve.mjs` 가 그에 맞춰 연다. 고른 방식은
      `server/data/tunnel.json` 에 남는다.
- [x] **③ 적용 UX** — 주소와 함께 QR 코드를 띄우고(`qrcode-terminal`), 모자란 것을
      딱 집어 알려준다(`npm run serve -- --check`). 서명 키도 첫 실행에 자동 생성.
- [ ] **`deploy-tunnel.ps1` 을 지울지 정하기** — `npm run serve -- --tunnel` 이 같은 일을
      하면서 OS 를 가리지 않고 QR 까지 준다. 남겨 두면 두 갈래를 다 관리해야 하고,
      배포 묶음에서는 이미 빼 두었다. 쓰던 사람이 없으면 지우는 편이 낫다.
- [x] Tailscale 경로 — 2026-09-16 실제로 켜서 밖에서 웹·로그인 확인. 이름 있는 터널은 여전히 안 써 봤다.
- [ ] 이름 있는 터널 경로는 **실제로 써 보지 않았다** — 계정·도메인이 없어
      판별 로직까지만 확인했다. 실제로 옮길 때 확인이 필요하다.
      **2026-09-14 점검(`npm run tunnel`):** cloudflared 2026.8.2 설치됨, Cloudflare 계정 인증 안 됨
      (`cert.pem` 없음), Tailscale 없음 → 빠른 터널만. **초대 링크에 서버 주소가 들어가므로, 빠른 터널을
      다시 켜면 이전에 보낸 초대 링크는 열리지 않는다** — 고정 주소의 값어치가 커졌다.

**검토 후 접음 — 누구의 서버든 `이름.도메인` (2026-09-15).** 서버마다 정형화된 주소를 주려면 도메인 구입이
필수라(listup.com·.app·.net·.io·.kr·.dev·.me 는 이미 남의 것) 하지 않기로 했다. 검토 내용: 경로 방식
(`도메인/이름`)은 모든 서버가 한 출처라 브라우저 저장소의 토큰이 서로 보여 안 됨 → 하위 도메인. 등록 서비스
(Workers)가 서버마다 터널을 만들어 주는 방식은 ① 무료 요금제가 공개 호스트로 사진·음악 등 큰 파일을 과하게
보내는 것을 금지해, 걸리면 계정의 모든 서버가 멈춤 ② 새 무료 zone 은 DNS 레코드 200개 ③ 모든 트래픽이 운영자
계정을 지남. 대안인 DDNS 방식(주소만 주고 각자 포트포워딩)은 약관 문제는 없지만 공인 IP·공유기 설정이 필요.
도메인 없이 개인 고정 주소가 필요하면 Tailscale Funnel(`*.ts.net`)이 남아 있다.

**내 서버는 Tailscale Funnel 로 고정 (2026-09-16).** 이 PC 를 `listup` 으로 등록해
**https://listup.tailffc023.ts.net** . `tailscale funnel --bg 4000` 이라 창을 닫거나 재부팅해도 남고(서버가
꺼져 있으면 502), `server/data/tunnel.json` 은 `tailscale`. `npm run serve -- --tunnel` 도 `--bg` 로 바꿨다.
Funnel 은 대역폭 제한(수치 비공개)이 있어 큰 파일을 많이 주고받으면 느릴 수 있다.

**미리 알아야 할 벽.** Cloudflare 이름 있는 터널은 **자기 도메인이 있어야** 한다
(계정에 등록된 zone 에만 DNS 를 붙일 수 있다). 도메인 구입은 문서로 없앨 수 없는 문턱이라,
안내를 두 갈래로 나누는 편이 정직하다.

| 상황 | 방법 | 문턱 |
| --- | --- | --- |
| 도메인 있음 | Cloudflare 이름 있는 터널 | 계정 + 기존 도메인 |
| 도메인 없음 | **Tailscale Funnel** | 계정만. `*.ts.net` 고정 주소를 무료로 준다 |
| 그냥 가끔 | 빠른 터널 (지금) | 없음. 주소가 매번 바뀜 |

**② 적용 기능에 들어갈 것**

- 지금 상태를 알아내기: 터널 없음 / 빠른 터널 / 이름 있는 터널 중 무엇인지
  (`~/.cloudflared/cert.pem` 과 `<UUID>.json` 존재 여부로 판별)
- 이름 있는 터널이 준비돼 있으면 `deploy-tunnel.ps1` 이 **알아서 그걸 쓴다**
  (지금은 `tunnel --url` 로 빠른 터널을 하드코딩)
- 정해진 호스트 이름을 저장해 두고 `LISTUP_CORS_ORIGIN` 에도 반영
- Tailscale 경로도 같은 자리에서 고를 수 있게 (터널 방식을 하드코딩하지 않는다)

**③ UX 에서 값어치가 큰 것**

- **QR 코드로 폰 연결** — 고정 주소가 생기는 순간 폰 설정이 "한 번 찍기"로 끝난다.
  주소를 손으로 입력하는 지금이 제일 성가신 부분이라, 여기가 체감 차이가 가장 크다.
  (`qrcode-terminal` 같은 작은 의존성 하나면 된다 — 지금은 QR 라이브러리가 없다)
- 준비물이 빠졌을 때 **무엇이 없는지 딱 집어 말하기** (계정 / 도메인 / 로그인 / DNS)
- 설정이 끝나면 실제로 붙는지 확인해 보여주기 (헬스체크 왕복)

**고정 주소가 생기면 따라오는 것들** — 이 작업의 값어치를 키우는 부분이다.

- 앱의 서버 주소를 한 번만 넣으면 된다
- `LISTUP_CORS_ORIGIN` 을 `*` 대신 실제 주소로 좁힐 수 있다
- **Cloudflare Access** 를 얹으면 터널 앞단에서 지정한 이메일만 통과시킬 수 있다 —
  로그인 화면조차 노출되지 않으므로 개인 서비스에는 rate limit 보다 강력하다
- Windows 서비스로 등록해 부팅 시 자동 시작 (`cloudflared service install`)

### 서버마다 계정을 새로 만들어야 하는 문제 — **서버별 계정으로 간다** (결정)

**2026-08-28 결정.** 정체성 통합(OAuth·자체 키)을 하지 않는다. 계정은 서버마다 따로 만든다.
대신 **앱이 서버를 여러 개 기억하게** 한다 → *서버 목록* (2026-09-14 완료, 아래 완료 절).
2026-08-27 에 코드를 직접 확인하며 정리한 아래 내용은, 왜 통합으로 가지 않는지의 근거로 남긴다.

**무엇이 문제였나.** 각 사용자가 자기 서버를 돌리는 구조라, A 의 서버와 B 의 서버는 서로
대화하지 않는다. B 가 A 의 저장소에 참여하려면 **A 의 서버에 계정을 새로 만들어야** 한다.
저장소를 몇 개 오가면 그때마다 새 비밀번호가 생긴다.

Minecraft 에 비유하면 통합되어야 하는 것은 "월드 데이터"가 아니라 **"정체성 확인"**이다.
Mojang 계정으로 아무 서버나 들어가지만 인벤토리는 서버마다 별개인 것과 같다.
**파일은 계속 서버별로 분리되어야 한다** — 그래야 각 서버 주인이 자기 것만 책임진다.

#### 왜 서버별로 두기로 했나

셋 다 이 프로젝트의 전제와 맞는 쪽이다.

- **계정 DB 가 서버 주인의 것이다.** 자가호스팅의 전제와 같다. 사설 게임 서버(이른바 프리섭)가
  저마다 계정을 두고, 그것이 자연스럽게 받아들여지는 것과 같은 모양이다. 게임에서도 통합되는
  것은 없고 서버를 고르는 화면만 있다.
- **복구 경로가 이미 사회적이다.** 초대 코드를 건네받아야만 들어오는 구조라 서버 주인과의
  채널이 항상 있다. 이메일 인증이 없는 것이 결함이 아니라 이 모델과 맞는다.
- **서버별 신원이 프라이버시상 이득이다.** 아래 C 검토의 단점 — "같은 키를 여러 서버에 쓰면
  서버 주인들끼리 대조해 동일인임을 알 수 있다" — 을 뒤집으면 지금 방식의 기능이다.
  이메일은 서버마다 다르게 쓸 수 있다.

#### 진짜 마찰은 계정 수가 아니었다

계정이 여러 개인 것은 비밀번호 관리자가 웹에서 이미 해결하는 문제고, 주력 경로가 웹이다.
실제로 성가신 것은 **앱이 한 번에 서버 하나만 가리킨다**는 것이다. 저장 키가 전부 단수라
(`listup.apiUrl` → `app/src/api/client.ts:46`, `listup.token`·`listup.user` →
`app/src/state/auth.tsx:14`) 주소를 바꾸면 세션을 지운다(`app/app/server.tsx:116`).
토큰을 발급한 서버 밖으로 내보내지 않으려는 올바른 조치지만, 결과적으로 서버를 오갈 때마다
주소 재입력 + 재로그인이다.

**정체성 통합은 이것을 고치는 가장 비싼 방법이었다.** 서버 목록은 클라이언트만 고치면 되고,
스키마·암호 라이브러리·`token_epoch` 를 전부 그대로 둔다.

#### 검토했던 선택지 셋 — 어느 것도 고르지 않았다

| | 방식 | 새로 운영할 것 | 문제 |
| --- | --- | --- | --- |
| A | OAuth (Google/GitHub 로그인) | 없음 | 구글에 의존 |
| B | 중앙 허브 서버를 직접 운영 | **평생 운영해야 함** | **탈락** |
| C | 자체 키 신원 (ed25519 서명) | 없음 | 아래 참조 |

**B 는 검토에서 뺐다.** 허브가 죽으면 모든 ListUp 서버의 로그인이 같이 죽고, 제작자가 그것을
영원히 켜둬야 하는 사람이 된다. "각자 자기 서버, 제작자 부담 0"이라는 이 프로젝트의 전제와
정반대다.

**표준 Passkey(WebAuthn)는 답이 아니다.** 명세가 자격증명을 도메인(RP ID)에 묶는다 —
피싱 방지를 위한 핵심 기능이다. 서버 주소가 `*.trycloudflare.com` 으로 매번 바뀌는 지금
구조에서는 같은 서버조차 주소가 바뀌면 못 쓴다. C 는 도메인에 묶이지 않는 **순수 키 쌍**을
사용자가 들고 다니는 방식이다(Nostr 나 SSH `authorized_keys` 와 같은 그림).

#### C 로 갈 때 풀어야 할 것 — 코드에서 확인한 순서

**1. 스키마가 막혀 있다. 그리고 이미 알려진 문제와 물려 있다.**

```
users.email         TEXT NOT NULL UNIQUE   ← 키만 있는 사용자는 만들 수 없다
users.password_hash TEXT NOT NULL          ← 마찬가지
```

SQLite 는 `NOT NULL` 을 떼려면 테이블을 재작성해야 하는데, 그게 이 문서 P3 의
**"마이그레이션이 `foreign_keys=ON` 트랜잭션 안에서 실행"** 항목이다(`server/src/db/index.ts`
의 `migrate()` 가 `BEGIN` 안에서 돌고, `openDb` 가 열 때 `foreign_keys = ON` 을 켠다).
`users` 를 참조하는 테이블이 6개다. **이 항목이 C 의 선행 조건이고, 순서가 강제되는 유일한 것.**

**2. 암호 라이브러리 선택이 Expo Go 를 쓸지 말지를 가른다.**

지금 `expo-secure-store`·`expo-crypto`·`@noble/ed25519` 가 모두 **미설치**다.

| 선택 | Expo Go |
| --- | --- |
| `@noble/ed25519` (순수 JS) | **유지된다** |
| `react-native-quick-crypto` (네이티브) | **깨진다 — 개발 빌드 필요** |

로그인 때 서명 한 번이면 되는 작업이라 순수 JS 로 충분하다. 네이티브를 쓰면 지금까지
모바일 확인에 써온 Expo Go 경로가 사라진다.

**3. 웹에서는 이점이 사라지고 오히려 나빠진다 — 가장 무거운 문제.**

`expo-secure-store` 는 웹에서 동작하지 않는다(Keychain/Keystore 에 해당하는 것이 브라우저에
없다). 웹에서는 개인 키가 `localStorage` 에 평문으로 놓인다. 그런데 **주력 경로가 웹이다** —
터널 주소를 브라우저로 여는 방식이고, 릴리즈 배포본도 웹이다.

| | 지금 (토큰) | C (개인 키) |
| --- | --- | --- |
| localStorage 에 있는 것 | 토큰 | **개인 키** |
| 새면 영향 | 그 서버, 30일 | **가입한 모든 서버, 영구** |
| 무효화 | `token_epoch` 로 즉시 | **방법 없음** |

**웹을 어떻게 다룰지가 사실상 결론을 정한다.** 웹까지 키로 가면 보안이 내려가고,
웹만 기존 방식으로 두면 "서버마다 새 계정" 문제가 웹에선 그대로 남는다. **이 질문이 미해결.**

**4. 되돌릴 수 없다 — `token_epoch` 와 정면으로 반대다.**

지금은 비밀번호를 바꾸면 세대가 올라가 유출 토큰이 전부 끊긴다(1.0 의 P0 로 만든 것).
키 방식엔 대응물이 없다. 키가 유출되면 새 키로 바꾸려 해도 **기존 키로 인증해야 하고**,
훔친 쪽도 똑같이 유효한 키를 갖고 있어 서버가 진짜를 구분하지 못한다. 유일한 해결은
**서버 주인이 DB 를 직접 고치는 것**이고, 여러 서버에 가입했다면 서버마다 각각 부탁해야 한다.

**5. 이메일이 하던 "누구인지 알아보는" 역할이 사라진다.**

`listMembers`(`server/src/services/repos.ts`)는 소유자에게만 이메일을 보여준다. 키 사용자는
이메일이 없어 `a3f9c2e1…` 만 남는다. 신원을 묶어주는 것은 **초대 코드를 건넨 그 순간**뿐이고,
나중에 "이 사람이 누구였지"를 확인할 방법이 없다. 표시 이름은 아무거나 넣을 수 있다.

**6. 나머지는 평범한 구현 일감.**

- 로그인 제한이 지금 `ip:` + `email:` 로 센다(`server/src/routes/auth.ts`). 키 로그인엔
  이메일이 없으니 `pubkey:` 기준을 더해야 한다.
- challenge nonce 를 서버가 기억하고 만료시켜야 한다. 단일 프로세스라 `RateLimiter` 처럼
  메모리 Map 으로 충분하다.
- **릴레이 방지**: 서명 대상에 그 서버의 식별자를 반드시 섞어야 한다
  (`"listup-auth:" + 서버ID + ":" + nonce`). 없으면 서버 A 가 받은 서명을 B 에 재전송해
  사칭할 수 있다.
- **프라이버시**: 같은 키를 여러 서버에 쓰면 서버 주인들끼리 대조해 동일인임을 알 수 있다.
  이메일은 서버마다 다르게 쓸 수 있지만 키는 재사용이 목적이라 불가능하다.

#### 검토했다가 접은 것 — "1기기 1계정 권장"

여러 기기 연결(QR 페어링)이 없어져 구현이 A 수준으로 가벼워지지만, **여러 기기가 그 자체로
백업 역할을 하던 것**을 잃는다. 폰을 잃으면 즉시 모든 서버에서 잠긴다. 또 iOS 는 키가
iCloud Keychain 으로 거의 자동 복구되는 반면 Android Keystore 는 내보내기를 막아
복구 문구를 직접 적게 해야 하는 비대칭이 생긴다. **쉬워지는 것이 안전해지는 것은 아니다.**

#### 이 결정을 뒤집게 된다면

서버 목록을 만들고도 "계정이 여러 개"가 실제로 걸리적거린다면 그때 다시 연다.
그때 밟을 순서는 그대로다.

1. 먼저 **웹에서 키를 쓸 것인지** 정한다. 이게 정해지면 나머지는 따라온다.
2. C 로 간다면 **P3 의 마이그레이션 항목을 먼저** 처리한다(스키마 재작성이 선행).
3. 기존 이메일+비밀번호와 **공존**시키는 것이 전제다. 스키마에 `public_key TEXT UNIQUE` 를
   더하는 방향이고, 기존 토큰 발급·`token_epoch` 로직은 그대로 재사용한다
   (바뀌는 것은 "비밀번호 대신 서명을 확인한다"는 앞단뿐).

### 로컬 폴더 동기화 (Android)

- [ ] **폴더를 하나 정해 두고 저장소와 견주어 보기** — VS Code 의 소스 제어 패널처럼,
  "무엇이 달라졌는지" 목록을 보고 방향(로컬로 덮기 / 서버로 덮기)을 골라 반영한다.
  지금은 파일을 하나씩 골라 올리는 것뿐이라 폴더째 다루는 방법이 없다. → `app/src/lib/files.ts`

  **왜 되는가.** 서버가 이미 콘텐츠 주소 기반이라 모든 파일에 해시가 붙어 있고
  (`FileEntry.blobHash`), Android 는 SAF 로 폴더 접근 권한을 영구 보관할 수 있다.
  설치된 `expo-file-system@57` 에 필요한 것이 다 있다:

  | 하는 일 | API |
  | --- | --- |
  | 폴더 권한 받기(영구) | `StorageAccessFramework.requestDirectoryPermissionsAsync()` |
  | 목록 읽기 | `StorageAccessFramework.readDirectoryAsync(dirUri)` |
  | 크기·수정시각·**MD5** | `getInfoAsync(uri, { md5: true })` |
  | 내용 읽기 / 쓰기 | `readAsStringAsync` / `createFileAsync` + `writeAsStringAsync` |

  **판별 방법 — 세 값을 견준다.** 서버의 `updatedAt` 은 *커밋된 시각*이지 로컬 파일의
  수정시각이 아니다. 그래서 둘만 비교하면 "다르다"까지밖에 알 수 없고, **어느 쪽이 새것인지**를
  모른다. 마지막으로 맞춘 시점을 기록해 두면 그때부터 3자 비교가 된다:

  ```
  기록(마지막 동기화):  경로 → { size, mtime, md5, serverBlobHash }
  로컬 바뀜? = size·mtime 이 기록과 다름 → 그때만 md5 로 확정
  서버 바뀜? = 지금 blobHash 가 기록의 serverBlobHash 와 다름
  ```

  | 로컬 | 서버 | 처리 |
  | --- | --- | --- |
  | 그대로 | 그대로 | 아무것도 안 함 |
  | 바뀜 | 그대로 | 로컬이 새것 → 올리기 |
  | 그대로 | 바뀜 | 서버가 새것 → 내려받기 |
  | 바뀜 | 바뀜 | **충돌 → 사람이 방향을 고른다** |
  | 없음 | 있음 | 기록에 있었으면 "로컬에서 지움", 없었으면 "아직 안 받음" |
  | 있음 | 없음 | 기록에 있었으면 "서버에서 지움", 없었으면 "새 파일" |

  기록이 없으면 이 구분이 안 된다 — 파일이 없는 게 "지운 것"인지 "받은 적 없는 것"인지
  알 수 없기 때문이다. 자동 병합을 하지 않는 이 프로젝트의 방침과도 맞다:
  애매하면 사람에게 묻는다.

  **1차 판별은 수정시각으로.** `size` + `mtime` 이 기록과 같으면 내용도 같다고 보고 건너뛴다
  (rsync·VS Code 와 같은 방식). 다를 때만 `getInfoAsync({ md5: true })` 로 확정한다.
  MD5 는 네이티브에서 계산하므로 파일을 JS 메모리로 읽지 않는다 — 100MB 파일도 견딘다.
  로컬 변경 판정에는 MD5, 서버 변경 판정에는 서버가 주는 SHA-256 을 쓰므로
  **JS 에서 해시를 계산할 일이 없다** (`expo-crypto` 도 필요 없다).

  **반영하는 법.**
  - 로컬 → 서버: 고른 것들을 **변경 제안 하나로 묶어** 올린다(편집 권한이면 직접 커밋 선택지도).
    이미 있는 `POST /repos/:id/blobs` → `POST /repos/:id/proposals` 흐름을 그대로 쓴다.
  - 서버 → 로컬: `GET /raw` 로 받아 `createFileAsync` + `writeAsStringAsync` 로 덮어쓴다.
  - 어느 쪽이든 끝나면 그 경로의 기록(size·mtime·md5·serverBlobHash)을 갱신한다.

  **서버는 손댈 필요가 없다.** 필요한 API 가 이미 전부 있다.

  **한계 — 미리 정해 둘 것.**
  - **Android 전용.** iOS 에는 SAF 같은 게 없고(보안 스코프 북마크는 Expo 가 노출하지 않음),
    모바일 웹도 File System Access API 가 없다. 다른 플랫폼에서는 이 화면을 숨긴다.
  - **자동 감지는 안 된다.** Android 는 앱이 백그라운드에서 폴더를 감시하게 두지 않는다.
    "새로고침 눌러 다시 훑기"가 최선이다.
  - 폴더 안 파일이 많으면 훑는 데 시간이 걸린다 — 진행 표시와 중단 수단이 필요하다.
  - 하위 폴더는 `readDirectoryAsync` 를 재귀로 돌아야 한다. 경로 깊이·개수 한도
    (`MAX_PATH_SEGMENTS`, `MAX_FILES_PER_REPO`)에 걸리는 경우를 처리해야 한다.
  - 기록은 폴더가 아니라 **앱 저장소(AsyncStorage)**에 둔다. 폴더 안에 두면 그 파일까지
    동기화 대상이 되고, 사용자가 지울 수도 있다. 키는 `저장소 id + 폴더 URI` 로.
  - 권한이 풀리는 경우(사용자가 회수, 기기 초기화)를 감지해 다시 요청해야 한다.

  **범위를 나눈다면** 1단계는 *로컬 → 서버 한 방향*만. 양방향·삭제 전파는 그다음.

## P2 — 사용성

- [ ] **제안 목록 페이지네이션** — 현재 전체를 한 번에 불러온다.
  (변경 이력은 커서 방식으로 바꿨으니 같은 방식을 쓰면 된다.)
- [ ] **용어·문구 통일** — "변경 제안하기/새 제안/변경 제안 만들기/제안 올리기", "병합/반영",
  가입 화면 제목("회원가입" vs "계정 만들기"), 만료일 표기 형식이 화면마다 다르다.
- [ ] **`docs/API.md` 와 실제 라우트 재대조** — 이번 변경(커서·용량 한도·ETag·400·`repo_blobs`)은
  반영했지만, 전체를 라우트별로 다시 훑은 적은 없다.
- [ ] **앱 접근성 마무리** — 목록 행에는 역할·라벨을 넣었지만 스크린리더로 실제 확인은 못 했다.
  라이트 테마 `accent`(#2f6df6) 는 `accentSoft`/`bg` 위에서 3.95/4.22 로 아직 AA 미달
  (활성 탭·필터 칩 글자). 다크는 통과.

## P3 — 나중에

- [ ] 제안이 올라오면 알림(푸시)
- [ ] 스토리지 백엔드 S3 호환 어댑터 (`BlobStore` 인터페이스만 교체하면 되도록 설계해 둠)
- [ ] 이메일 인증 (비밀번호 재설정은 운영자 발급 코드로 했다 — SMTP 없이)
- [ ] **파일 미리보기** — **범위 밖으로 본다** (2026-09-14, "순수 파일 공유"). 파일 내용을 앱 안에서
  쓰는 기능(재생·미리보기)은 하지 않는다. 다시 연다면 서버의 `?inline=1`·Range 는 준비되어 있다.
- [ ] 앱 단위 테스트 (지금은 서버 테스트만 있음)
- [ ] 저장소 검색 / 파일 검색
- [ ] 제안에 이미 담긴 파일을 다시 담을 때 교체할지 묻기
- [ ] **마이그레이션이 `foreign_keys=ON` 트랜잭션 안에서 실행** — 테이블 재작성이 필요한 변경을
  못 한다. 마이그레이션 동안만 FK 를 끄고 끝에 `PRAGMA foreign_key_check`. `server/src/db/index.ts`
  (데이터 변환용 `run(db)` 훅은 이번에 들어갔다.)
- [x] ~~LICENSE 파일·`license` 필드 없음~~ — **AGPL-3.0 으로 정했다** (2026-08-27).
  누가 베껴다 닫힌 유료 서비스로 파는 것을 막는 것이 목적. 자가호스팅 서버 도구의
  표준 선택이고(Nextcloud·Mastodon), 직접 의존성이 전부 MIT 라 제약도 없었다.
  개인이 설치해 쓰는 것은 배포가 아니라 의무가 생기지 않는다.
- [ ] **웹 번들 1.7MB** — 아이콘 글리프맵 전 종류가 들어간다. 압축은 터널(Cloudflare)이 해 준다.
- [ ] **라우트 보일러플레이트** — 권한 확인·파싱이 라우트마다 반복된다. 스키마 검증 도입은 취향 문제.
- [ ] **제안 목록·상세의 `conflicts` 설명** — 이제 "제안 뒤 바뀐 파일" 말고 "파일/폴더 이름 충돌"도
  섞여 들어가는데, 앱 배너 문구는 앞의 경우만 설명한다.

---

## 검증 대기

### v1.2.0 — 폰에서 확인할 것 (2026-09-14)

웹에서는 같은 서버 경로를 자동으로 확인했다(e2e 6·7·8). 폰에서만 도는 코드가 남는다.
**APK 를 새로 설치한 뒤(앱 아이콘도 이때 처음 보인다):**

- [ ] 앱 아이콘 — 홈 화면·설정의 앱 목록, 안드로이드 13 테마 아이콘
- [ ] 로그인 정보 저장 — 저장 켜고 로그인 → 앱 완전 종료 → 다시 열면 자동 로그인 (`expo-secure-store`)
- [ ] 큰 파일(100MB 넘는 것) 올리기 — 진행률이 오르고, 터널 주소에서도 올라가는지 (`FileHandle.readBytes`)
- [ ] 올리는 도중 비행기 모드 → 풀기 → 다시 올리면 이어서 가는지
- [ ] 폴더 올리기 — 폴더 선택기(`Directory.pickDirectoryAsync`)로 하위 폴더까지 경로가 유지되는지
- [ ] 파일 내려받기 → (처음 한 번) 저장 폴더 고르기 → 그 폴더에 저장됨. 같은 이름이면 "이름 (1)" (v1.2.1)
- [ ] 폴더 zip 내려받기 → 저장 폴더에 zip
- [ ] 초대 화면의 **공유** 버튼 → 메신저로 링크 보내기 → 그 링크를 폰 브라우저로 열기 → "설치한 앱에서 열기"
      로 앱이 열려 서버 추가 → 가입 → 참여
- [ ] 비밀번호를 잊었나요? → 코드로 새 비밀번호
- [ ] v1.1.0 이 깔린 폰에서 새 버전 알림이 뜨고, 새 APK 가 **덮어 설치**되는지(같은 서명 키)
- [ ] 다크 모드 표시
- [ ] **내 폴더와 비교 (안드로이드)** — 폴더 선택기로 고른 폴더를 훑어 개수가 맞는지, "저장소에만"을 받으면 그 폴더 안에
      하위 폴더가 만들어져 들어가는지(다시 훑기로 0), "내 폴더에만"을 올리기

### PC 앱 — 직접 설치해서 확인할 것 (2026-09-16)

흐름 테스트(`npm run test:desktop`)가 개발 묶음과 설치본(win-unpacked)에서 창·서버 켜기/끄기·들어가기·초대 링크
주소·재설정 코드·창 닫아도 서버 유지·백업·포트 충돌을 확인했다. 사람이 보고 OS 와 닿는 것이 남는다.

- [ ] 설치 파일 실행 → SmartScreen "추가 정보 → 실행" → 설치 위치 고르기 → 시작 메뉴·바탕화면 바로가기, 앱 아이콘
- [ ] 트레이 아이콘·메뉴(열기 / 서버 켜기·끄기 / 끝내기), 창을 처음 닫을 때 알림
- [ ] **Tailscale Funnel 모드** — 지금 명령줄 서버가 4000 을 쓰므로, 명령줄 서버를 끄고 PC 앱에서 포트 4000 +
      Tailscale 로 켜서 https://listup.tailffc023.ts.net 이 PC 앱 서버로 가는지. 데이터 폴더를 `server/data` 로
      고르면 테스트 계정이 그대로
- [ ] 빠른 터널 모드 — 주소가 뜨고, 서버를 끄면 닫히는지
- [ ] PC 브라우저로 초대 링크를 열고 "설치한 앱에서 열기"(`listup://join?...`)를 누르면 PC 앱이 열려 서버 추가·참여로 이어지는지
- [ ] Windows 에 로그인하면 켜기 → 재부팅 뒤 트레이에서 서버가 켜져 있는지
- [ ] 내려받기(저장 대화상자)·폴더 올리기·로그인 정보 저장 뒤 다시 켜면 자동 로그인
- [ ] 새 버전 설치 파일로 덮어 설치 — 서버 목록·설정·데이터가 남는지
- [ ] 다크 모드 (Windows 설정을 따른다)

### 서버 목록 — 네이티브 (2026-09-14)

웹(Edge, 서버 두 개)에서는 아래를 자동으로 확인했다: 예전 키 이전(로그인 유지), 첫 실행 로그인
직행, 서버 추가 → 바로 들어가기, 전환 왕복 재로그인 없음, 토큰이 발급한 서버로만 감, 다른 곳에서
비밀번호를 바꾸면 그 서버만 풀림, 꺼진 서버 전환은 오류만, 지우기·기본 서버 보호·이름 바꾸기,
앱을 열 때 로그인 안 됨 + 서버 여럿이면 목록부터, 로그아웃은 그 서버만, 전환 뒤 뒤로 가기로
이전 서버 화면에 닿지 않음. 스크립트는 저장소에 두지 않았다(앱 단위 테스트가 없어서, P3).

- [ ] Expo Go 에서 목록·추가·전환 — **네이티브가 주 사용처인데 아직 안 돌려 봤다**
- [ ] 네이티브에서 `router.dismissAll()` + `replace('/')` 뒤 안드로이드 뒤로 가기 버튼 동작
- [ ] 개발 중 LAN IP 가 바뀌어도 기본 서버가 새 IP 를 따라가는지

### 최신 검증 — 개선 작업 (2026-08-27)

- [x] `npm test` — 서버 테스트 106개 통과 (66 → 106)
- [x] `npm run typecheck` — 서버 + 앱 통과
- [x] 실제 서버 기동 후 직접 확인: 가입 → 저장소 생성 → 업로드 → 다운로드,
      한도 초과 업로드 413(아무것도 커밋 안 됨), ETag + `If-None-Match` 304,
      비밀번호 오류 400, 파일/폴더 이름 충돌 409, 정적 서빙 + 보안 헤더,
      `/.env` 404, SPA 라우트 200, 잘못된 `LISTUP_PORT` 로 기동 실패,
      모르는 `LISTUP_*` 경고, `npm run backup`
- [ ] `npm run build:web --workspace @listup/app` — 이번 변경 뒤로는 안 돌려 봤다
- [ ] 브라우저 흐름 재확인 — 인증 가드·보호 라우트·서버 주소 화면이 새로 들어갔다

### 클린 클론 검증 — `83b861f` 기준 통과

푸시된 브랜치를 새로 clone 해서 처음부터 돌린 결과. 남이 받아서 실행해도 되는 상태다.

- [x] `npm install` — 약 38초, 실패 없음
- [x] `npm test` — 서버 테스트 66개 통과
- [x] `npm run typecheck` — 서버 + 앱 통과
- [x] `npm run build:web --workspace @listup/app` — 웹 번들 생성 성공
- [x] `npm run server` 기동 후 API 왕복 확인
- [x] 브라우저 흐름 (가입 → 저장소 생성 → 업로드 → 초대 → 참여 → 제안 → 병합) — `78645f4` 시점

### 실사용 시작 — 음악 라이브러리 (2026-08-27)

처음으로 진짜 데이터를 넣고 다른 기기에서 썼다. 여기까지가 "만든 것"과 "쓰는 것"의 경계다.

- [x] `C:\Users\hoyaj\Music\Musics` 의 mp3·lrc **539개 · 2,192MB** 적재 (38초)
      → `scripts/sync-folder.mjs`. 폴더 구조·일본어 파일명 유지, 다운로드 바이트 일치 확인
- [x] 다른 PC 에서 터널 주소로 접속 — 로그인·폴더 탐색·음악 다운로드 정상
- [x] 환경변수 없이 저장소 총량 4GB 기본값이 적용되는지 확인 (2.2GB 저장소에 추가 업로드 201)

이때 잡은 것: 경로를 NFC 로 정규화하지 않으면 분해형(NFD)으로 저장된 이름(`ROSÉ`)이
매번 다른 파일로 취급돼 계속 다시 올라간다. mp3 였다면 24MB 를 매번 재전송했을 것이다.

측정된 것: 파일 539개 → `snapshot_entries` 145,540행 → DB 48MB.
파일당 스냅샷을 만드는 구조라 **행 수가 파일 수의 제곱에 비례**한다(N²/2).
이 규모에서는 문제없지만 수천 개가 되면 스냅샷 묶기가 필요하다.

### 실기기 — Android 외부망 (2026-08-27)

폰을 다른 네트워크(LTE)에 두고 확인했다. 공개 경로 두 개를 열었다:
API·웹은 Cloudflare 빠른 터널, Metro 는 Expo 터널(`npm run app -- --tunnel`, `@expo/ngrok`).

- [x] Expo Go 로 앱이 뜨고 동작함 — **처음으로 네이티브에서 실행해 봤다**
- [x] Android Chrome 에서 웹앱 접속
- [x] **서버 주소 화면** — 터널 모드에서는 접속 주소(`…exp.direct`)로 API 를 유추할 수 없으므로
      앱에서 직접 지정해야 하는데, 이 경로가 실제로 동작했다. 이 화면을 만든 이유가 이것이다.
- [x] 공개 주소로 API 왕복 (가입 → 저장소 → 한글 파일 업로드 → 되읽기 → 초대 발급)

- [ ] `expo-document-picker` 로 고른 파일이 네이티브에서 업로드되는지 — **개별 확인 안 됨**
- [ ] `File.downloadFileAsync` + 공유 시트로 저장이 되는지 — **개별 확인 안 됨**
- [ ] 다크 모드 표시 — **개별 확인 안 됨**
- [ ] 같은 공유기(LAN)에서 서버 주소 자동 감지가 동작하는지 — 외부망으로 확인해서 이 경로는 미검증

> 위 세 항목은 "전체적으로 잘 된다"는 확인만 받았고 하나씩 짚어보지는 않았다.
> 파일 선택·저장(`app/src/lib/files.ts`)은 웹과 네이티브 구현이 갈리는 지점이라
> 따로 확인하는 편이 안전하다.

### 로컬에서 받아 돌리는 법

```bash
git clone -b claude/file-sharing-collab-platform-tvo6dg https://github.com/yh-yeop/ListUp.git
cd ListUp && npm install
npm run server     # 터미널 1 — http://localhost:4000
npm run app        # 터미널 2 — 브라우저는 w, 모바일은 QR
```

Node 22 이상이 필요하다. `better-sqlite3` 는 네이티브 모듈이지만 13.x 부터 N-API 바이너리가 패키지 안에 들어
있어 Node·Electron 버전을 가리지 않고 설치 스크립트 없이 된다 (11.x 시절에는 Node 24 용 프리빌드가 없어
빌드 도구가 없으면 설치가 깨졌다 — 그래서 12.x, 다시 PC 앱 때문에 13.x 로 올렸다).

---

## 완료

- [x] **내 폴더와 비교** — 2026-09-17. 기기의 폴더를 그때그때 골라 저장소 폴더와 **파일 이름으로** 견준다(자동 감시 없음,
  폴더를 기억하지 않음). 경로까지 같아야 같은 파일 / "폴더 구조 무시하고 이름만" 스위치, 크기가 다르면 표시(내용 해시는
  안 봄). "저장소에만"은 골라 받기, "내 폴더에만"은 골라 올리기(편집자는 커밋 하나, 열람자는 변경 제안 하나).
  - PC 앱: main 이 훑고·읽고·그 폴더에 폴더 구조대로 받아 쓴다(`desktop/src/folders.ts`). 대화상자로 고른 폴더 밖은 거절.
  - 안드로이드: SAF 폴더 선택기로 훑고, 캐시에 받아(이어받기) 하위 폴더를 만들어 넣는다. **폰에서 확인 전.**
  - 브라우저: `webkitdirectory` 로 훑고 올리기는 되지만 폴더에 쓸 수 없어 받기는 브라우저 내려받기로. iOS 는 버튼을 숨긴다.
  - 서버: `GET /repos/:id/files?recursive=1` — 폴더 아래 파일을 한 번에(없는 서버면 앱이 폴더마다 물어 모은다).
  - 확인: 서버 158, 웹 흐름 160(비교 13 — 경로/이름만 개수, 크기 다름, 브라우저 받기, 편집자 올리기, 열람자 제안),
    PC 앱 흐름 29(비교 8 — 폴더에 구조대로 받기, 임시 파일 없음, main 이 읽어 올리기, 폴더 밖·안 고른 폴더 거절).
  - 로컬 폴더 동기화(아래 P1, 3자 비교·기록)의 가벼운 앞 단계다 — 기록 없이 이름·크기만.

- [x] **터널 뒤 로그인 제한이 모두를 막던 문제** — 2026-09-16. 터널(cloudflared·Tailscale Funnel)은 같은 PC 에서
  붙어 모든 요청의 소켓 주소가 127.0.0.1 이라, 누가 로그인을 10번 틀리면 IP 기준 제한이 접속자 전원을 15분 막았다.
  `LISTUP_TRUST_PROXY` 기본값을 `loopback`(이 PC 에서 붙은 프록시의 `X-Forwarded-For` 만 믿음)으로, 값은 `0`·`1`·
  대역/IP/CIDR 목록. Funnel 에 헤더 확인용 경로를 잠깐 걸어 **Funnel 이 실제 공인 IP 를 넣고 접속자가 보낸
  `X-Forwarded-For` 는 버린다**는 것을 확인했다. cloudflared 는 뒤에 덧붙이므로 맨 뒤의 믿지 않는 주소를 쓴다(테스트).
  공유기 안에서 직접 붙는 사람은 루프백이 아니라 헤더를 믿지 않는다. 서버 테스트 157개. → `server/src/config.ts`

- [x] **파일 공유 다듬기 (v1.2.0)** — 2026-09-14. 계획·단계는 [docs/PLAN-SHARING.md](docs/PLAN-SHARING.md).
  - **검증** — 웹 흐름 테스트를 저장소로(`e2e/`, `npm run test:e2e`, 빈 포트·임시 DB), GitHub Actions CI
    (타입 검사 → 서버 테스트 → e2e, 실패하면 스크린샷을 올림).
  - **규모** — 스냅샷을 바뀐 것만 기록(`repo_files` + `snapshot_changes`, 마이그레이션 v5). 음악 DB 사본으로
    551개 스냅샷 전부 이전 전후 목록 일치 확인. 여러 파일을 커밋 하나로(`files/commit`), 동기화 스크립트도.
  - **전송** — 나눠 올리기(8MB, 끊기면 이어서, 세션 DB 보관, 마이그레이션 v6)·진행률, 한 파일 기본 한도
    100MB → 2GB, `Range`/`If-Range` 이어받기, 짧게 사는 다운로드 링크(브라우저·OS 다운로드), 폴더 올리기
    (웹 `webkitdirectory`, 안드로이드 폴더 선택기), 폴더 zip(ZIP64).
  - **나누기** — 초대 링크(`/join?code=`, `listup://join?server=&code=`), 로그인·가입 뒤까지 코드를 들고 가기,
    공유기 안 주소 경고. 운영자 발급 비밀번호 재설정 코드(`npm run reset-password`, 마이그레이션 v7).
  - **실서버 DB 는 서버를 다시 켤 때 v4 → v7 로 옮겨진다.** 켜기 전에 `npm run backup`.

- [x] **서버 목록 — 서버를 고르는 화면** — 2026-09-14. *서버마다 계정* 결정(2026-08-28)에서
  나온 것. 계획은 [docs/PLAN-SERVER-LIST.md](docs/PLAN-SERVER-LIST.md), 계획과 달라진 점도 거기에.
  - 저장 키 셋(`listup.apiUrl`·`token`·`user`)을 `listup.servers` 하나로. 예전 키는 첫 실행에
    옮긴다 → `app/src/state/servers.ts`
  - `setApiBaseUrl`/`setAuthToken` → `setApiTarget(url, token)` 하나. 401 은 그 요청의 서버·토큰이
    지금과 같을 때만, 그 토큰을 가진 서버만 로그아웃 → `app/src/api/client.ts`
  - 목록 `app/app/servers.tsx`, 추가/수정 폼 `app/app/server.tsx`, 기본 서버는 지울 수 없음
  - 서버 전환은 부팅처럼(로딩 화면 → 토큰 확인 → `_layout` 이 스택을 비우고 index 로)
  - 시행착오 기록: 전환 뒤 이전 서버 화면을 걷어내는 방법 셋(화면에서 `dismissAll`, 사용자를
    내려 보호 라우트에 맡기기, Stack `key` 재마운트)이 모두 타이밍에 따라 틀리거나 효과가
    없었다. `key` 를 바꿔도 내비게이션 기록은 컨테이너에 남아 복원된다. 목록을 늘 스택 맨
    아래에서 여는 것으로 해결

- [x] 서버 기반 (SQLite 스키마·마이그레이션, 콘텐츠 주소 blob 저장, scrypt + HMAC 토큰) — `2a5d386`
- [x] 저장소 / 파일 / 스냅샷 API — `2a5d386`
- [x] 초대 코드 API (발급·미리보기·참여·회수, 만료·횟수 제한) — `2a5d386`
- [x] 변경 제안 API (생성·댓글·충돌 감지·병합·닫기) — `2a5d386`
- [x] 서버 테스트 66개 — `2a5d386`
- [x] Expo 앱 화면 일습 (로그인·저장소·파일·초대·제안·멤버·이력) — `78645f4`
- [x] 문서 (README, `docs/ARCHITECTURE.md`, `docs/API.md`) — `78645f4`
- [x] better-sqlite3 `^11.5.0` → `^12.2.0` — Node 24 용 프리빌드가 없어 `npm install` 이
  깨지던 문제. 서버 테스트 66개 통과로 호환 확인. — 2026-08-19
- [x] 웹 정적 빌드를 서버가 같은 오리진으로 서빙 — `@fastify/static` + SPA fallback,
  `LISTUP_WEB_DIR` 로 조정. `EXPO_PUBLIC_LISTUP_API_URL=/` 빌드와 조합하면 프로세스 하나로
  웹+API 가 나가서 배포가 단순해진다. — 2026-08-19
- [x] 첫 공개 배포 (Cloudflare 빠른 터널) — 공개 URL 에서 가입·로그인·저장소 생성·업로드·
  되읽기·초대 발급 왕복과 웹 로그인 흐름까지 확인. `deploy-tunnel.ps1` 추가. — 2026-08-19

- [x] **코드 리뷰 기반 개선 묶음** — 2026-08-27. 8개 관점 리뷰 → 회의적 검증 → 수정 → 재리뷰.
  테스트 66 → 106개.

  *서버*
  - 업로드 한도 초과 파일이 잘린 채 커밋되던 P0 버그 (multipart 가 에러 없이 자르고 `truncated`
    만 세우는 것을 검사) → 413, 아무것도 커밋 안 됨
  - 동시 업로드 lost update — 트랜잭션 안에서 head 재조회
  - `repo_blobs` 도입 — 해시만 알면 남의 저장소 파일을 제안으로 읽을 수 있던 문제 차단
  - 저장 용량 한도 (저장소 총량 / 사용자별 하루 스테이징), 병합 시 파일 수 한도 재검사
  - 경로 기준 `/raw` 에 ETag + `no-cache`, `snapshot` 지정 시에만 immutable, HEAD 는 스트림 안 엶
  - 파일/폴더 이름 충돌 검사(`findPathConflict`)를 업로드·이동·제안 생성·병합에 적용
  - `normalizePath` NFC 정규화 + 세그먼트 trim + format 문자 거부, 기존 데이터 마이그레이션(v3)
  - scrypt 비동기화, 없는 계정도 더미 해시 검증(응답 시간 노출 차단),
    비밀번호 오류 401 → 400 (앱이 로그아웃하던 문제)
  - 추방·강등된 사람의 초대 무효화 + 자동 회수
  - blob 쓰기 `flush`, 크기 불일치 시 교체, 기동 시 임시 파일 정리
  - 설정값 검증(잘못되면 기동 실패), 모르는 `LISTUP_*` 경고, `trustProxy`, 로그 레벨,
    초대 코드 로그 마스킹, 종료 타임아웃, 500 응답에 `requestId`
  - CORS 목록 trim + credentials 제거, 정적 서빙 닷파일 차단·캐시 헤더·보안 헤더
  - `npm run backup` (WAL 안전 백업), `tsx` 를 dependencies 로, FK 자식 인덱스,
    이력 커서를 `(created_at, id)` 복합으로
  - `deploy-tunnel.ps1` 헬스체크 대기 + 잘못 빌드된 번들 자동 재빌드

  *앱*
  - 토큰 복원 중 401 로 로그아웃되던 P0 버그, 오프라인이어도 세션 유지, 인증 가드(보호 라우트)
  - 서버 주소 변경 화면 (연결 확인 후 저장, 주소가 바뀌면 세션 삭제 — 토큰이 다른 호스트로
    가지 않게), 릴리스 빌드에서 서버를 바꿀 수 있게 됨
  - 저장소 이름·설명 수정, 소유권 넘기기, 파일·폴더 이름 변경, 과거 시점 보기, 이력 더 보기
  - 권한에 맞는 버튼 노출(제안 닫기·초대 회수), 병합된 제안은 제안 당시 원본을 내려받게
  - 업로드 사전 검사(서버 한도 조회), 덮어쓰기 확인, 실패 원인 표시
  - 제안 작성 중 이탈 경고, 탐색 중인 폴더에 담기, 중복 파일 key 충돌
  - 대비 AA 충족(textMuted/textFaint/warning), 입력 포커스 표시, 접근성 역할·라벨,
    요청 타임아웃 30초, 죽은 코드 제거

- [x] **외부망 Android 실기기 확인** — 2026-08-27. Cloudflare 터널(API·웹) + Expo 터널(Metro)로
  다른 네트워크의 폰에서 접속. 네이티브 앱을 처음으로 실행해 봤고, 터널 모드에서 필수인
  "서버 주소" 화면이 실제로 제 몫을 했다. `npm run app` 이 LAN IP 를 직접 고르고
  (`scripts/start-app.mjs`) `-- --tunnel` 을 넘길 수 있게 정비했다.

- [x] **P0 3종 — 1.0 전에 반드시 하기로 한 것** — 2026-08-27
  - **로그인 요청 제한** — 비밀번호 대입을 막을 장치가 전혀 없었다. 실패를 이메일 기준과
    IP 기준으로 따로 세고, 한도를 넘으면 일정 시간 막는다(429 + 남은 초). 성공하면 기록을
    지워 정상 사용자는 걸리지 않는다. 단일 프로세스라 메모리에 둔다. → `server/src/lib/rate-limit.ts`
  - **비밀번호를 바꿔도 기존 토큰이 유효하던 문제** — 토큰이 무상태(HMAC)라 개별 폐기가 안 됐다.
    사용자마다 세대(`users.token_epoch`, 마이그레이션 v4)를 두고 토큰에 실어, 비밀번호를 바꾸면
    세대를 올려 이전 토큰을 한 번에 끊는다. 바꾼 기기는 응답으로 받은 새 토큰으로 이어 간다.
  - **참조되지 않는 blob 정리(GC)** — 버려진 업로드가 디스크에 영원히 남았다. 스냅샷·제안 어디에서도
    참조하지 않고 유예 시간(기본 24시간)이 지난 것을 지운다. DB 를 먼저 정리하고 파일을 지우며,
    중간에 끊겨 남은 고아 파일도 다음 번에 줍는다. 기본 6시간마다 자동, `npm run gc` 로 수동.
    → `server/src/services/gc.ts`

### 고친 버그 (기록)

- 초대 코드가 소진된 뒤 **이미 참여한 사람이 다시 열면 거부**되던 문제.
  사용 가능 여부를 멤버 확인보다 먼저 보고 있었다. 순서를 뒤집어 해결. — `2a5d386`
- 가로 스크롤 탭(`RepoNav`)이 세로로 늘어나 **아래 버튼 클릭을 가로막던** 레이아웃 문제. — `78645f4`
- Git Bash 에서 `EXPO_PUBLIC_LISTUP_API_URL=/` 로 웹을 빌드하면 MSYS 경로 변환이 `/` 를
  `C:/Program Files/Git` 로 바꿔 번들에 박히던 문제. PowerShell 에서 빌드하도록 README 에
  명시해 회피. Metro 캐시는 환경변수 변경을 못 알아채므로 `--clear` 가 필요했다. — 2026-08-19
